<% if section.length>0 %>
<tbody>
  <tr>
    <th colspan="4"><div class="sectionHeadingbar"><%= title %></div></th>
  </tr>
  <%= render :partial => 'entry', :collection => section %>
</tbody>
<% end %>