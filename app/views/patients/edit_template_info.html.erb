<% remote_form_for(:patient, @patient,
                   :url => patient_path(@patient),
                   :method => 'put',
                   :update => 'templateInformationContainer') do |f| %>
    <%= f.text_field :name %>
<% end %>
