<span class="paren">(</span>
<div class="inline-fraction">
    <span id="numeratorValue" class="numeratorValue"><%= numerator %></span>
    <span class="separator"></span>
    <span id="denominatorValue" class="denominatorValue"><%= denominator %></span>
</div><span class="paren">)</span>