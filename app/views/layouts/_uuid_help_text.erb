<div id="uuid-<%= controller.class %><%= @is_vital_sign if @is_vital_sign %>" style="display:none">
Format is either UUID or OOID.
<br />
UUIDs must be represented in the form XXXXXXXX-XXXX-XXXX-XXXXXXXXXXXXXXXX, where each X is a character from the set [A-Fa-f0-9].
<br />
OIDs must be represented in dotted decimal notation, where each decimal number is either 0, or starts with a non-zero digit.  More formalls, an OID must be in the form ([0-2])(.([1-9][0-9]*|0))+.  OIDs must be no more than 64 characters in length.
</div>
