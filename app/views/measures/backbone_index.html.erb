<script type="text/javascript">
  $(function() {
    PopHealth.selectedMeasures = new PopHealth.SelectedMeasures(<%== @measures %>);
    PopHealth.categories = PopHealth.Utils.bootstrapCategories(<%== @categories %>, PopHealth.selectedMeasures, $("#main"));
    PopHealth.Utils.bootstrapFilters(<%== @filters.to_json %>, PopHealth.selectedMeasures);

  	// PopHealth.Utils.bootstrapDemographics("Race", <%= raw(@races.to_json) %>)
  	// PopHealth.Utils.bootstrapDemographics("Ethnicity", <%= raw(@ethnicities.to_json) %>)
  	// PopHealth.Utils.bootstrapDemographics("Gender", <%= raw(@genders.to_json) %>)
  	// PopHealth.Utils.bootstrapDemographics("Language", <%= raw(@languages.to_json) %>)
  })
 </script>

<div id="sidebar" class="span2">
</div>
<div id="main" class="span10">
<!-- 	<div id="measures" class="row-fluid">
      <div class="dashboard">
        <div class="pull-right">
        	<% if (@selected_provider || !can?(:manage, :providers)) %>  	
           	<a class="btn" href="#">Export Report</a>
        	<% else %>
        		<div class="btn-group">
							<a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
  							Export Report
  							<span class="caret"></span>
							</a>
							<ul class="dropdown-menu">
								<li><a href="#" tabindex="-1">Provider Report</a></li>
								<li><a href="#" tabindex="-1">Practice Report</a></li>
							</ul>
						</div>
        	<% end %>
        </div>
        <% if (@selected_provider) %>
          <dl>
            <dt>Provider:</dt>
            <dd class="practiceName"><h4><%=@selected_provider.full_name%></h4></dd>
          </dl>
        <% else %>
          <%= content_tag(:h1, current_user.company) unless current_user.company.nil? || current_user.company.empty? %>
          <dl>
            <dt>Practice:</dt>
            <dd class="practiceName"><h4><%=APP_CONFIG['practice_name']%></h4></dd>
          </dl>
        <% end %>
        
        <dl>
          <dt>Reporting Period:</dt>
          <dd>
            <span id="measurementPeriodStartDate"><%= Time.at(@period_start).strftime("%m/%d/%Y") %></span> - 
            <input type="text" class="input-small" value="<%= Time.at(@effective_date).strftime("%m/%d/%Y") %>">
            <a style="margin-bottom:9px" class="btn" <% if (@selected_provider) %> data-npi="<%=@selected_provider.npi%>" <% end %>><i class="icon-refresh"></i></a>
          </dd>
        </dl>
        <dl>
          <dt># of Patients: </dt>
          <dd><%= @patient_count %></dd>
        </dl>
        <% unless (@selected_provider) %>
          <dl>
            <dt># of Providers: </dt>
            <dd><%= Provider.all.count %> </dd>
          </dl>
        <% end %>
      </div>
      <hr>
      <ul class="nav nav-pills">
      </ul>
  
      <%#= render partial: "measure", collection: @alt_measures %>
  </div> -->
</div>
